# BLACK RED hosting

Deploy as a Node.js Web Service.

Build command:
```bash
npm install
```

Start command:
```bash
npm start
```

Required environment variables:
- QWEN_API_KEY
- FREEIMAGE_KEY

After deploy, open the service URL. The same Node.js server serves index.html and /api/generate.
